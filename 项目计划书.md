# 单词记忆游戏平台 · 项目开发计划书

## 一、项目定位

纯前端、本地优先、AI 按需生成内容的单词记忆游戏平台。无后端，无服务器，所有数据存 IndexedDB。后续部署只需静态托管 + 反向代理。

AI 支持两种模式：

- 直连模式：用户自填 API Key，前端存 IndexedDB，明确密钥风险。
- 代理模式：`aiBaseUrl` 指向反向代理，前端不存 Key，由代理注入。

---

## 二、技术选型

| 层 | 选型 |
|---|---|
| 框架 | React 18 + TypeScript |
| 构建 | Vite |
| 数据库 | Dexie.js（IndexedDB） |
| 状态 | Zustand |
| 算法 | ts-fsrs |
| 样式 | Tailwind CSS |
| 路由 | React Router v6 |
| 压缩 | pako |

---

## 三、目录结构

```text
src/
├── components/
│   ├── common/          Button, Dialog, Input, Loading...
│   ├── cards/           卡片组件
│   ├── games/           游戏规则组件
│   └── layout/          布局
├── pages/
│   ├── Home.tsx
│   ├── AddWord.tsx
│   ├── Review.tsx
│   ├── WordLibrary.tsx
│   ├── Practice.tsx
│   ├── PracticeResult.tsx
│   └── Settings.tsx
├── db/
│   ├── index.ts         Dexie 实例
│   └── migrations.ts    迁移注册表
├── services/
│   ├── ai.ts            大模型调用
│   ├── queue.ts         单词队列
│   ├── mutations.ts     统一写入口
│   ├── cards.ts         单词库 CRUD
│   ├── export.ts
│   ├── import.ts
│   └── json.ts
├── stores/
│   ├── cardStore.ts
│   ├── gameStore.ts
│   └── settingsStore.ts
├── algorithms/
│   ├── fsrs.ts          FSRS 封装
│   └── fsrsAdapter.ts   ReviewState ↔ FsrsCard 适配
├── games/
│   ├── index.ts
│   ├── cloze.ts
│   └── reading.ts
├── prompts/
│   ├── index.ts         默认提示词 + 类型
│   ├── card.ts          卡片生成默认提示词
│   ├── games.ts         完型/阅读默认提示词
│   └── resolve.ts       读取设置覆盖 + 变量替换
├── types/
│   └── index.ts
└── utils/
    ├── uuid.ts
    └── validate.ts
```

---

## 四、数据结构

所有持久化结构都带 `schemaVersion`。时间统一毫秒时间戳。ID 统一 uuid。字段 camelCase。

### 4.1 Card

```ts
type CardSource = 'user_added' | 'ai_generated' | 'imported';

interface Card {
  id: string;
  word: string;
  meaning: string;
  pronunciation: string;
  humorExplanation: string;
  roots: string;
  mnemonic: string;
  examples: { en: string; zh: string }[];
  tags: string[];
  source: CardSource;
  createdAt: number;
  updatedAt: number;
  deletedAt?: number;
  schemaVersion: number;
}
```

### 4.2 ReviewState

持久化层使用 camelCase + 毫秒。进入 `ts-fsrs` 前通过适配层转换。

```ts
type Rating = 1 | 2 | 3 | 4; // Again=1, Hard=2, Good=3, Easy=4
type ReviewStateName = 'new' | 'learning' | 'review' | 'relearning';

interface ReviewState {
  cardId: string;
  due: number;
  stability: number;
  difficulty: number;
  elapsedDays: number;
  scheduledDays: number;
  reps: number;
  lapses: number;
  state: ReviewStateName;
  lastReview: number;
  lastRating: Rating;
  updatedAt: number;
  schemaVersion: number;
}
```

### 4.3 FsrsCard 适配

`ts-fsrs` 使用 snake_case，且 `due`、`last_review` 为 `Date`。必须适配，不做直接持久化。

```ts
interface FsrsCard {
  due: Date;
  stability: number;
  difficulty: number;
  elapsed_days: number;
  scheduled_days: number;
  reps: number;
  lapses: number;
  state: number;
  last_review?: Date;
}

function toFsrsCard(state: ReviewState): FsrsCard;
function fromFsrsCard(cardId: string, card: FsrsCard): ReviewState;
```

### 4.4 Question

使用判别联合。阅读理解不需要 `blanks`，且 `translation` 必填。完型与阅读的每个空/子题都带 `wordId`，支持按子题反推评分。

```ts
type QuestionType = 'cloze' | 'reading';

interface Blank {
  index: number;
  answer: string;
  options: string[];
  wordId: string;
}

interface ReadingSubQuestion {
  index: number;
  stem: string;
  options: string[];
  answer: string;
  wordId: string;
}

interface QuestionBase {
  id: string;
  type: QuestionType;
  gameRule: QuestionType;
  wordIds: string[];
  explanation: string;
  translation?: string;
  highlightedWords: { wordId: string; text: string; start: number; end: number }[];
  difficulty: number;
  source: 'ai_generated' | 'imported';
  createdAt: number;
  schemaVersion: number;
}

interface ClozeQuestion extends QuestionBase {
  type: 'cloze';
  content: { passage: string; blanks: Blank[] };
  translation?: string;
}

interface ReadingQuestion extends QuestionBase {
  type: 'reading';
  content: { passage: string; questions: ReadingSubQuestion[] };
  translation: string;
}

type Question = ClozeQuestion | ReadingQuestion;

interface QuestionDraft {
  content: ClozeQuestion['content'] | ReadingQuestion['content'];
  translation?: string;
  explanation: string;
  highlightedWords: QuestionBase['highlightedWords'];
}

interface GradeResult {
  isCorrect: boolean;
  perItem: Record<number, boolean>;
  wordRatings: Record<string, Rating>;
}
```

### 4.5 Attempt

用于统计、详情、历史记录。

```ts
interface Attempt {
  id: string;
  questionId: string;
  answers: {
    index: number;
    answer: string;
    isCorrect: boolean;
    wordId?: string;
  }[];
  isCorrect: boolean;
  durationMs: number;
  createdAt: number;
  schemaVersion: number;
}
```

### 4.6 Settings

```ts
type PromptKey = 'card' | 'cloze' | 'reading';

interface PromptTemplates {
  card: string;
  cloze: string;
  reading: string;
}

interface Settings {
  id: 'settings';
  aiProvider: string;
  aiApiKey: string;
  aiModel: string;
  aiBaseUrl: string;
  aiMode: 'direct' | 'proxy';
  targetRetention: number;
  reviewBatchSize: number;      // 默认 20，范围 1-100
  clozeCount: number;           // 默认 15
  readingCount: number;         // 默认 5
  gameRulesEnabled: QuestionType[];
  libraryView: 'card' | 'list';
  prompts: PromptTemplates;
  schemaVersion: number;
}
```

若已有 v1 数据，`schemaVersion` 升到 2，迁移时补默认提示词。

---

## 五、Dexie 建表

```ts
this.version(1).stores({
  cards: 'id, word, createdAt, updatedAt, deletedAt, *tags',
  reviewStates: 'cardId, due, state',
  questions: 'id, type, createdAt, *wordIds',
  attempts: 'id, questionId, createdAt',
  settings: 'id',
});
```

Dexie `version()` 与记录内 `schemaVersion` 分开管理。旧版本导入跑迁移，新版本拒绝。

---

## 六、核心机制：单词队列

队列 = `review_states` 按 `due` 升序的视图，关联 `cards` 取内容，并过滤 `deletedAt`。

### 6.1 队列更新点

| 入口 | 触发 | 影响 |
|---|---|---|
| 录入单词 | `addCard` | 新词进队，`due=now` |
| 复习评分确认 | `rateCard` | 更新 `due`，词往后排 |
| 刷题提交 | `rateCardsByQuestion` | 按子题批量更新涉及单词 |
| 删除单词 | `deleteCard` | 软删除，队列过滤，题目保留 |
| 导入单词库 | `importCards` | 批量进队 |

不触发队列的操作：编辑卡片内容、生成题目、识别模式点击、刷现有题读取。

### 6.2 实时更新

所有写操作走 `services/mutations.ts`。队列封装在 `services/queue.ts`，同时监听 `reviewStates` 和 `cards`。

```ts
const queue$ = liveQuery(async () => {
  const states = await db.reviewStates.orderBy('due').limit(200).toArray();
  const cards = await db.cards.bulkGet(states.map(s => s.cardId));
  return states
    .map((state, i) => ({ state, card: cards[i] }))
    .filter((x): x is { state: ReviewState; card: Card } => !!x.card && !x.card.deletedAt);
});
```

队列查询加 `limit`，默认 200。Home 今日待复习数用 `count()`，不用 `limit(200)`。

### 6.3 复习快照

进入复习时一次性取一批，锁定，不中途变化。退出后重新取最新队列。

---

## 七、功能模块

### 7.1 录单词

页面：`AddWord.tsx`

流程：

```text
输入单词 + 意思（可选）
→ AI 生成卡片内容（加载中）
→ 卡片预览（可编辑任意字段）
→ 编辑 / 重新生成 / 录入单词库 / 丢弃
→ 自动进入下一个
```

规则：

- 重新生成覆盖当前预览，不存库。
- 入库时创建 `review_state`，`due = now`，`state = 'new'`。
- 丢弃无二次确认，保留最近一次可撤销。撤销栈为内存栈，刷新丢失。
- 编辑内容不触发 AI，不改队列。

### 7.2 回顾单词

页面：`Review.tsx`

流程：

```text
选数量（1-100，默认 20）
→ 库存不足则提示并取最大值
→ 识别模式：显示英文
→ 点“记得”或“不记得”
→ 进入单词卡界面
→ 选“真记得”或“卡了没记住”
→ 更新权重
```

评分映射：

| 识别模式 | 卡片二次选择 | Rating |
|---|---|---|
| 记得 | 真记得 | Easy |
| 不记得 | 真记得 | Good |
| 记得 | 卡了没记住 | Hard |
| 不记得 | 卡了没记住 | Again |

规则：

- 识别模式只给英文，不显示中文。
- 无论点哪个都进卡片界面。
- 卡片界面的二次选择才更新权重。
- 按 `due` 升序取前 N 个。
- 复习时快照锁定，不中途变。

### 7.3 单词库

页面：`WordLibrary.tsx`

默认排序：`createdAt` 降序，最新录入在最前。  
默认视图：卡片视图，一张一张左右切换。  
视图切换：卡片视图 ↔ 列表视图，偏好存 `settings.libraryView`。

卡片视图交互：

- 左箭头/左滑 → 上一条（更新的）
- 右箭头/右滑 → 下一条（更早的）
- 键盘 ← →
- 底部显示「第 N / 总数」
- 边界禁用箭头，显示「已是最新」「已是最早」
- 切换范围限定当前筛选结果内

列表视图功能：

- 搜索：按单词、意思模糊匹配
- 标签筛选：多选
- 排序切换：录入时间 / 更新时间 / 下次复习时间
- 多选批量删除
- 虚拟滚动

增：库内点「+」快捷添加，复用录入组件。  
改：所有字段可改，不重新调 AI。提供「重新生成」按钮，需二次确认。改内容不改队列。  
删：单个/批量删除，二次确认。软删除，设置 `deletedAt`。题目保留，评分跳过已删除词。批量删除用事务。

### 7.4 刷题

页面：`Practice.tsx` + `PracticeResult.tsx`

入口：

```text
刷题
 ├── 生成新题
 └── 刷现有题
```

生成新题：

```text
选择题型（完形填空 / 阅读理解）
→ 从单词队列取队首 N 个
   （完型默认 15，阅读默认 5，可在设置改）
→ 单词放入提示词
→ AI 生成题目
→ 考察单词在题目中高亮
→ 作答 → 提交 → 详情页
```

规则：

- 生成后存 `questions` 表，带 `wordIds`。
- 题目生成后不弹出队列。
- 阅读理解必须返回译文，存 `translation`。
- 完形填空：一段短文，挖 N 个空，每空 4 选项，正确答案是队列单词。
- 阅读理解：短文 + 选择题，考察单词高亮。
- 每个空/子题都带 `wordId`。

作答与提交：

```text
逐题作答 → 全部答完 → 提交
→ 详情页：
   1. 阅读理解的译文
   2. 每道题详细解释
   3. 你的答案 vs 正确答案
   4. 答题结果
```

刷现有题：

```text
按题目权重升序排序
权重 = 题目关联单词的 FSRS stability 聚合（取最小值）
空 wordIds 的题目放最后
→ 按顺序出题
→ 答完提交
```

从题目反推单词更新：

- 按子题 `wordId` 分别评分。
- 答对 → Good。
- 答错 → Again。
- 批量写用事务，避免多次重排。
- 已删除单词跳过评分。

### 7.5 分享

导出单词库 / 题目库 JSON，可导入还原。导出用 pako 压缩成 `.json.gz`。

导出格式：

```json
{
  "format": "wordapp-data",
  "schemaVersion": 1,
  "exportedAt": 0,
  "count": 500,
  "data": {
    "cards": [],
    "reviewStates": [],
    "questions": [],
    "attempts": []
  }
}
```

不导出 `settings.aiApiKey`。导出 `settings` 时保留 `prompts`。

导入规则：

- 校验 `format` 和 `schemaVersion`。
- 旧版本跑迁移，新版本拒绝。
- 冲突处理：跳过 / 覆盖 / 新建 ID。
- 新建 ID 时必须重映射：`cards.id`、`reviewStates.cardId`、`questions.wordIds`、`attempts.questionId`。
- 导入时询问是否覆盖当前提示词。

---

## 八、AI 服务层

`services/ai.ts`：

```ts
export async function callAI(
  prompt: string,
  settings: Settings,
  signal?: AbortSignal
): Promise<string>;

export async function generateCard(
  word: string,
  meaning: string,
  settings: Settings
): Promise<Partial<Card>>;

export async function generateQuestion(
  rule: GameRule,
  words: Card[],
  settings: Settings
): Promise<QuestionDraft>;
```

要求：

- 从 `settings` 读 key / baseUrl / model / mode。
- 支持 OpenAI 兼容接口。
- JSON 清洗（去 markdown 包裹）。
- 支持超时、取消、重试、限流。
- 错误类型：
  - `AIConfigError`
  - `AIFormatError`
  - `AINetworkError`
  - `AITimeoutError`
  - `AIAbortError`
  - `AIRateLimitError`

---

## 九、提示词系统

### 9.1 默认提示词

`prompts/index.ts`：

```ts
export const DEFAULT_PROMPTS: PromptTemplates = {
  card: '...',
  cloze: '...',
  reading: '...',
};
```

### 9.2 解析与渲染

`prompts/resolve.ts`：

```ts
export function resolvePrompt(key: PromptKey, settings: Settings): string {
  return settings.prompts[key]?.trim() || DEFAULT_PROMPTS[key];
}

export function renderPrompt(
  template: string,
  vars: Record<string, string | number>
): string;
```

规则：

- 设置页有自定义值 → 用自定义。
- 空字符串 → 用默认。
- 变量统一 `{{var}}`。
- 保存前校验必要变量，缺失则禁止保存并提示。

必要变量：

| 提示词 | 必要变量 |
|---|---|
| card | `{{word}}`、`{{meaning}}` |
| cloze | `{{words}}`、`{{count}}` |
| reading | `{{words}}`、`{{count}}` |

### 9.3 AI 调用接入

```ts
export async function generateCard(
  word: string,
  meaning: string,
  settings: Settings
): Promise<Partial<Card>> {
  const template = resolvePrompt('card', settings);
  const prompt = renderPrompt(template, { word, meaning });
  // ...
}

export async function generateQuestion(
  rule: GameRule,
  words: Card[],
  settings: Settings
): Promise<QuestionDraft> {
  const template = resolvePrompt(rule.id, settings);
  const prompt = rule.buildPrompt(words, template);
  // ...
}
```

---

## 十、FSRS 封装

`algorithms/fsrs.ts`：

```ts
export function createInitialState(cardId: string): ReviewState;
export function applyRating(
  state: ReviewState,
  rating: Rating,
  now: number,
  targetRetention: number
): ReviewState;
```

`algorithms/fsrsAdapter.ts`：

```ts
export function toFsrsCard(state: ReviewState): FsrsCard;
export function fromFsrsCard(cardId: string, card: FsrsCard): ReviewState;
```

字段与 `ts-fsrs` 通过适配层对齐。时间用毫秒时间戳。

---

## 十一、游戏规则接口

`games/index.ts`：

```ts
interface GameRule {
  id: QuestionType;
  name: string;
  description: string;
  minWords: number;
  maxWords: number;
  buildPrompt(words: Card[], template: string): string;
  parseResponse(raw: string): QuestionDraft;
  grade(question: Question, answer: Record<number, string>): GradeResult;
}
```

首批实现：`cloze`、`reading`。  
提示词集中 `prompts/`，`GameRule.buildPrompt` 接收已解析的模板，不直接读默认提示词。

---

## 十二、页面清单

| 页面 | 职责 |
|---|---|
| Home | 今日待复习数、快捷入口、统计概览 |
| AddWord | 录入单词，预览、编辑、重新生成、入库/丢弃 |
| Review | 选数量 → 识别模式 → 卡片模式 → 更新权重 |
| WordLibrary | 单词库 CRUD、卡片/列表视图、左右切换 |
| Practice | 刷题入口：生成新题 / 刷现有题 |
| PracticeResult | 提交后详情：译文、逐题解释、答案对比 |
| Settings | AI 配置、数量设置、提示词编辑、数据管理 |

空状态必须引导：无单词→引导添加；无 key→引导去设置；无题目→引导去生成。

### Settings 页面

职责：

- AI 配置：provider、apiKey、model、baseUrl、mode。
- 数量设置：`reviewBatchSize`、`clozeCount`、`readingCount`、`targetRetention`。
- 提示词编辑：卡片生成、完形填空、阅读理解三组。
  - 每组多行文本编辑。
  - 显示可用变量说明。
  - 保存、重置当前项、恢复全部默认。
  - 保存时校验必要变量，缺失则禁止保存。
  - 修改后立即生效，下一次 AI 调用使用新提示词。
- 数据管理：导入、导出、清空。

---

## 十三、开发顺序

| 步骤 | 内容 | 验收 |
|---|---|---|
| 1 | 骨架 + 路由 | 页面能切换 |
| 2 | 数据库 + 类型 + 适配层 | 能手动增删查，ReviewState 可转 FsrsCard |
| 3 | AI 服务 + 提示词 | 能生成卡片 JSON；设置页可修改提示词并生效；空值回退默认；缺失必要变量禁止保存 |
| 4 | 录单词闭环 | 输入→生成→编辑→入库 |
| 5 | FSRS + 队列 | 能取队列、更新状态 |
| 6 | 回顾单词 | 识别→卡片→更新权重 |
| 7 | 单词库 | CRUD + 卡片视图左右切换 + 软删除 |
| 8 | 生成新题 | 完型/阅读，高亮，作答，详情 |
| 9 | 刷现有题 | 按权重排序出题，按子题反推评分 |
| 10 | 导入导出 | 单词库/题目库 JSON 互导，冲突重映射 |
| 11 | 打磨 | 空状态、错误、响应式、统计 |

里程碑：

- M1（步骤1-2）：骨架+数据库+适配层
- M2（步骤3-4）：能加词，AI生成卡片，提示词可改
- M3（步骤5-6）：能按记忆曲线复习
- M4（步骤7）：单词库完整
- M5（步骤8-9）：能刷题
- M6（步骤10）：能导入导出

---

## 十四、Agent 编码约定

- 所有类型集中 `types/index.ts`。
- 数据库操作走 `db/`，组件不直接碰 Dexie。
- AI 调用走 `services/ai.ts`，组件不直接 fetch。
- 提示词集中 `prompts/`，运行时优先读 `settings.prompts`。
- AI 调用统一走 `resolvePrompt + renderPrompt`，组件不直接拼接提示词。
- 设置页可改提示词，不硬编码。
- 提示词模板必须校验必要变量。
- 写操作走 `services/mutations.ts` 统一入口。
- 取词走 `services/queue.ts`。
- 字段 camelCase，时间统一毫秒时间戳，ID 统一 uuid。
- 所有持久化结构带 `schemaVersion`。
- `ReviewState` 与 `ts-fsrs` 通过 `fsrsAdapter` 转换。
- 题目生成后不弹出单词队列。
- 刷题答对答错按子题反推涉及单词 FSRS 状态，答对 Good，答错 Again。
- 批量写用事务。
- 提示词要求返回 `highlightedWords`。
- 阅读理解必须返回 `translation`。
- 数量设置集中 `Settings`，不硬编码。
- 单词库默认 `createdAt` 降序。
- 卡片视图支持键盘、鼠标、触摸左右切换。
- 切换范围限定当前筛选结果内。
- 编辑内容不触发 AI、不改队列。
- 删除单词使用软删除，题目保留，评分跳过已删除词。
- 视图偏好存 `settings`。
- 复习时快照锁定，不中途变。
- 队列查询加 `limit`，默认 200。
- Home 今日待复习数用 `count()`。
- 导入冲突新建 ID 时必须重映射所有引用。
- 导出不包含 `aiApiKey`，导出 `settings` 保留 `prompts`。
- 不做部署配置，留 `aiBaseUrl` 口，并支持直连/代理两种 AI 模式。